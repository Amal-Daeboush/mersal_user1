/* import 'package:flutter/material.dart';
import 'package:get/get.dart';
import 'package:mersal/core/constant/app_image_asset.dart';
import 'package:mersal/data/model/service_model.dart';
import 'package:mersal/view/my_service/view/myservice_screen.dart';

import '../../../data/model/category_model.dart';
import '../../home/widgets/card_top_service.dart';
import 'custom_service.dart'; */

/* class CustomListViewCardService extends StatelessWidget {
  const CustomListViewCardService({super.key});

  @override
  Widget build(BuildContext context) {
  

    return ListView.builder(
      itemCount: service.length,
      shrinkWrap: true,
      scrollDirection: Axis.horizontal,
      itemBuilder: (context, index) {
        return InkWell(
          focusColor: Colors.transparent,
          hoverColor: Colors.transparent,
          highlightColor: Colors.transparent,
          splashColor: Colors.transparent,
          onTap: () => Get.off(MyserviceScreen()),
          child: CustomService(
            isSelected: false,
            image: service[index].image,
            name: service[index].name,
          ),
        );
      },
    );
  }
}
 */