


/* class AddCityData {
  Crud crud;
  AddCityData(this.crud);
  putData({required String city}) async {
    var response =
        await crud.postData('ApiLinks.addcity', {"city": city, '_method': 'PUT'});
    return response.fold((l) => l, (r) => r);
  }
} */
